# Ideas raised and not acted on

**Written by `scripts/ideas.py`. Recording is automatic and needs no
permission; ACTING still goes through the roll or an explicit ask.**

Not the backlog: that holds small OWED jobs and refuses anything that
cannot say why it is owed. An idea is not owed.

| id | raised | status | idea | why it was not acted on |
|---|---|---|---|---|
| IDEA1 | 2026-08-23 | OPEN | Read the two remaining recommended fields: metascience/replication crisis (Ioannidis; Gelman & Loken) and safety engineering/human factors (Reason's Swiss cheese; Perrow) | Recommended to the user in conversation and recorded only in T181; the third field became T179/T180 but these two have no owner and no next step |
| IDEA2 | 2026-08-23 | OPEN | Decide whether 32% method overhead means the machinery is load-bearing or that the project has substituted method for progress | I raised it when the user asked what the second body of work was, said I could not judge it from inside, and neither of us returned to it |
| IDEA3 | 2026-08-23 | OPEN | Ask whether the quantity rule generalises past durations — A371's 9:5 was a COUNT stated from memory and no check catches that | Named as T178's own stated limit; the fix would be a habit, and habits are what this project keeps finding do not hold |
| IDEA4 | 2026-08-23 | OPEN | Reframe T165 or retire it: the sibling-port arm is all that remains and every available comparator ships stock microcode | T176 answered the comparator question without a clone, which changed what T165 could still learn; the item is parked on U10 and its remaining shape was never re-examined |
| IDEA5 | 2026-08-23 | CLOSED 2026-08-24: APPROVED by the user on the status page, 2026-08-24 — and it was ALREADY BUILT: check_ledger.py gates the L2 nag on L1 blocks (comparing audit-log.md's L1 count against l1_digested) rather than on the date, with a deliberate fail-towards-nagging fallback. Approval recorded here so the click is not lost when the page is regenerated | Gate the L2 audit nag on new L1 blocks existing, not on the date changing | Raised by the user noticing daily tasks piling up; L2 already handles an empty window gracefully, so only the NAG is wrong |
| IDEA6 | 2026-08-24 | CLOSED 2026-08-24: DONE for the labelling rows (T191, 2026-08-24) — sectioned rendering built as a VIEW, lossless over all 579 bodies. Left open in the general case: the ledger READER still shows one paragraph, and the ~31% of entries with no CAPS headings are unaddressed | Render ledger entries as SECTIONS rather than one paragraph — split on the CAPS lead-ins that already act as headings (69% of entries have at least one, median 3), as a VIEW over the file rather than a rewrite of 579 rows | Raised by the user 2026-08-24 looking at the labelling rows: 'they're kind of just walls of text'. Measured but not built — and the first measurement of it was WRONG (counted bold runs, which shred sentences mid-clause); the corrected one is what the number above rests on. Needs a decision on whether the ~31% with no headings get left as-is or hand-fixed |
| IDEA7 | 2026-08-24 | OPEN | Ask whether entry LENGTH is degrading my own reading, not just the user's — the concrete instance is T189, where I lifted an unmeasured '~10 minutes' out of the middle of a long row and cited it as grounds | The user raised the possibility 2026-08-24 and it is the sharper half of their question. I have ONE clean instance, which is not a rate; nobody has looked for others, and check_ledger's length nag fires on 338 entries at once, which is noise rather than a signal |
| IDEA8 | 2026-08-24 | OPEN | STACK vs METHOD has a systematic boundary problem the category spec does not address: an INSTRUMENT'S SETTING is chosen by POLICY. Capture granularity is a tool capability AND a token-budget decision, so the same fact is defensible in either category | Raised by the user 2026-08-24 while labelling A164 — 'the stack not taking enough screenshots, but also the method dictating that we shouldn't be overly granular so as to conserve token usage'. Not acted on because resolving it WOULD BE labelling entries in the live sixteen, which is the one thing I must not do while U10 is open. It may also explain part of the reader-level disagreement in A375 |
| IDEA9 | 2026-08-24 | OPEN | Register ares captures against our own reference frames so classify_recording.py can compare the two builds frame-by-frame mechanically — the machinery exists (dHash, --fps 0, labelled scene-refs); the missing piece is that ares frames are a different emulator at a different window size and need registering first | The user proposed the comparison 2026-08-24 believing the only routes were heavy token spend or them hand-annotating both runs. Neither is true — classify_recording.py's own docstring names registration as the single blocker. Not acted on because it needs a roll and nobody has priced the registration step |
| IDEA10 | 2026-08-24 | OPEN | Report T165's count on its OWN binary question — ABOUT-THE-GAME vs NOT — rather than as GAME:STACK. On the 14 unanimous entries that is 7:7, dead level, where the GAME:STACK framing reads 7:5 and sounds like a lean | Noticed 2026-08-24 while testing whether collapsing the categories would help the user label. Whether METHOD entries count as evidence of 'we are slow' is a JUDGEMENT, not arithmetic, so this is FLAGGED and not applied — an entry about the audit ladder is not obviously evidence about recompilation difficulty |
| IDEA11 | 2026-08-24 | OPEN | Bounded config-deviation pass over BanjoRecomp and Zelda64Recomp: diff our toml/build setup against the toolchain authors' own usage, plus read Zelda's mods framework as the one worked example of extending these ports | User asked 2026-08-24 whether the MM-style audit is worth repeating there. Visited set CHECKED first (A400's lesson): T143 read their READMEs; no deeper pass exists. Surface is thin — no patches/, no technical reference — so a full audit would find little; the deviation check is the part with value and it is one checkpoint |
| IDEA12 | 2026-08-24 | CLOSED 2026-08-24: Promoted to user queue as U13 (2026-08-24) — the user asked to review each proposal before launch; the queue row carries the full proposal and the decision now lives there, not here | Emulator-side STATE tracing as a ground-truth oracle: script BizHawk/mupen Lua to log our known addresses (guard byte 0x80068A95, scene state, clear behaviour) per frame on the unmodified ROM | Never considered — 0 ledger hits for BizHawk; T1 only killed gdb-ATTACH on ares, which is a different instrument. It answers questions our build cannot: does the REAL game stop colour-clearing at the title (A396's contradiction dissolves if yes), and how does the guard byte behave when the game works (A211). No changes to our build at all |
| IDEA13 | 2026-08-24 | CLOSED 2026-08-24: Promoted to user queue as U14 (2026-08-24) — same promotion; one decision surface, per the day's two-copies lesson | Symbol transfer from the MM port's map to our anonymous functions: 4,794 semantically NAMED symbols (Thread_MainProc, DMA_ReadSync, Video_Clear_Framebuffer) from a same-studio engine, matchable by structure/constants against our boot_func_XXXXXXXX | Never considered — 0 hits for signature matching or symbol transfer. Treasure may have reused engine code between 1997 and 2000; even partial transfer labels our unknowns, and their map literally names a framebuffer-clearing function while we hunt an invisible clear. Compiler drift may defeat byte-exact matching; structural matching is the fallback |
| IDEA14 | 2026-08-24 | CLOSED 2026-08-24: Promoted to user queue as U15 (2026-08-24) — same promotion; the upstream-adjacency concern is stated in the queue row | Read the public RT64 and N64Recomp issue trackers for known bugs matching ours (clear/buffer behaviour, F3DEX2 quirks, stalls) | Never done — 0 hits. T36/T38 close SENDING upstream; reading public trackers is passive and inbound. Known-issue lists are where 'their bug is our bug' can be checked without importing hypotheses blind |
| IDEA15 | 2026-08-28 | OPEN | Check overlay RESIDENCY at tutorial time by fingerprinting 0x800E515C in a590-tutorial.rdram against ovlfile05's expected words (A643's method) | Needs a source of expected raw instruction words; the recompiled tree carries mnemonics, not encodings. overlay_map.py gives load addresses but not content. Deferred in the A670 DFS in favour of the static B-vs-C selector inside ovlfile05, which needs no fingerprint. |
| IDEA16 | 2026-08-28 | OPEN | Confirm scene 5 IS the tutorial/corridor rather than inferring it from the texture-bank base address | T101: scene identity has been wrong twice from sampling. The 0x80206AE0 match is strong but it is one address. Needs either a user-confirmed frame or a scene-id read from a dump. |
| IDEA17 | 2026-08-28 | OPEN | Scenes 6-10 also load large data files overlapping the 0x206000-0x234000 bank region | Each scene resets the allocation cursor so they share memory. If scene 5 is NOT the tutorial, the same bank could belong to scene 6/7/8/9/10 and the identification collapses. Not checked. |
| IDEA18 | 2026-08-28 | CLOSED 2026-08-28: ANSWERED. A memory scan finds all four of ovlfile19's table-B functions as DATA-RESIDENT pointers in a590-tutorial.rdram: 0x800E5C68 at 0x8010376C, 0x800E5834 at 0x8010378C, 0x800E5078 at 0x801037AC, 0x800E5A8C at 0x801037FC -- a cluster on a 0x20 stride around 0x80103770-0x80103800, plus a second cluster at 0x8013C7D4-0x8013C854. In the scene-20 dump (a680) all four have ZERO hits, i.e. the overlay is not resident. POSITIVE CONTROL that could have failed and did not: ovlfile19_func_800E4BF0, scene 19's init, is found at exactly 0x800591E8 = T_INIT + 18*4 -- index 18, scene 19, exactly where overlay_map's table says it must be, in BOTH dumps. So the dispatch table A670's control proved instruction scanning cannot see is at ~0x80103770 with a 0x20 stride, and that is where to read next. UNVERIFIED: the record layout at that stride, and what indexes it. Read in the last minutes of a timed session; no ledger entry. | Find the overlay DATA jump table that dispatches ovlfile19's four table-B functions (0x800E5078/5834/5A8C/5C68) | HARD STOP for static analysis, and the limit is MEASURED not assumed: scene 19's init 0x800E4BF0 is a known function pointer living in T_INIT (a data table) and appears 0 times as an addiu immediate anywhere in the tree. So instruction scanning cannot see data-resident pointers by construction. Would need Yay0 decompression of the overlay's data section, or a runtime trace. |
| IDEA19 | 2026-08-28 | OPEN | Runtime-trace indirect call targets instead of scanning for them | The recompiled tree funnels indirect calls through a dispatch helper; logging target vram addresses per task would name what actually calls the B-touching functions and when. Not attempted -- it is a probe + build + run, and the session was spent on the static route which turned out to hit a characterised wall. |
| IDEA20 | 2026-08-28 | OPEN | Diff ovlfile05 against ovlfile19: they load IDENTICAL data (same 4 files, same vram) and differ only in init function | Discovered while disambiguating the tutorial scene. Two overlays over the same assets is a strong hint they are variants of one level (e.g. tutorial vs normal play). Comparing what ovlfile19 does that ovlfile05 does not could isolate the tutorial-specific path. Not pursued -- found at the very end of the DFS. |
| IDEA21 | 2026-08-28 | OPEN | Read the REFERENCE's values at 0x80070918 and 0x800709C0, not just its access counts | A630 counted 5,443 reads at each but never recorded WHAT was read. The values would say whether they are pointers into the missing texture bank (0x206000-0x234000) or something else entirely, which decides whether table B is even the right thread. Needs an ares watch that logs values; ARES_WATCH_READ currently counts. |
| IDEA22 | 2026-08-28 | CLOSED 2026-08-28: PARTIALLY ANSWERED, and the shape is now clear. Table A's accessor boot_func_8002CFAC (545 instr, 3 callers) reaches it by INDEXING: it takes a 7-bit field from its argument's byte at +0x06 (lbu, andi 0x7F), shifts it left 2, and indexes a static table based at 0x80059280. The emitter reaches tables B/C by dereferencing a POINTER held in a record's +0x08 field (A679). Two different mechanisms -- so the caller-profile similarity (A 73% vs B 75% zero-static-caller) was never capable of explaining why A is read in the tutorial and B is not, and the hypothesis it killed deserved killing. UNVERIFIED: the 0x80059280 table's identity, and whether the 7-bit index is scene-independent (which would explain A's steady 31/task in both scenes). Read at the very end of a timed session; no ledger entry; verify before use. | Explain why table A is read during our tutorial (31/task) while table B is not, given both are ~74% zero-static-caller | This KILLED the indirect-dispatch hypothesis mid-session: if the caller profile explained B's silence, A would be silent too, and it is not. Whatever distinguishes them is unknown and is the cleanest remaining static question on this thread. |
| IDEA23 | 2026-08-28 | OPEN | Identify scene 2 -- the PREVIOUS scene before the tutorial in both the tutorial and freeze dumps | scene_id.py now reads it (0x80068A93). Scene 2 -> 19 is the transition into the tutorial. If scene 2 is the attract/demo, that pins the whole task-number banding used by A660/A668/A669 to real scene boundaries instead of inherited guesses (T101). |
| IDEA24 | 2026-08-28 | OPEN | The freeze is NOT a scene change -- tutorial and freeze dumps are both scene 19 with prev 2 | Falls straight out of scene_id.py and was not the question being asked, so it is recorded rather than chased. It constrains the freeze thread: whatever hangs the game at ~205 s happens WITHIN scene 19, not during a transition out of it. |
| IDEA25 | 2026-08-28 | OPEN | Resolve the scene-20 naming collision across the ledger | A672 measured that scene 20 is BOTH what the inherited task bands call 'the attract' AND what a594-logo.rdram was named for. One scene, two labels, used interchangeably in entries. Needs a decision about which name is right (and possibly a user-watched frame), not a guess -- so it is recorded rather than settled. |
| IDEA26 | 2026-08-28 | CLOSED 2026-08-28: DONE. The [rad], [gate] and [arg] per-task lines in events.cpp are now gated on their probe having fired (snp_rad_first / snp_gate_first / snp_arg_first). Verified on a 60 s run with all three probes removed: 0 lines of each, while the live [scene] probe still emits 1,774 -- so the gate suppresses the stale ones without suppressing a working one. rad_hist.py refuses the log with rc=2. T163 satisfied: the noise is fixed at source, not only in the consumer. | Suppress the [rad] and [gate] per-task lines at SOURCE when their probe never fired | A673 fixed the stale-probe trap in the TOOL (rad_hist.py now refuses a log lacking the FIRST OBSERVATION marker), which closes the wrong-answer path. But events.cpp still emits 6,169 all-zero [rad]/[gate] lines into every log recorded without the probes, which is a trap for any other reader -- a human, or an ad-hoc grep. The source fix is two conditions on snp_rad_first/snp_gate_first. Not done: it needs a rebuild and verification, and the session clock was better spent on the write-up. T163 says fix noise at source, so this is owed. |
| IDEA27 | 2026-08-28 | CLOSED 2026-08-28: DONE properly: added as section 7 of docs/observation-checklist.md, which observed_run.sh prints BEFORE launching, so the question actually reaches the user instead of sitting in the ideas file. Phrased so that 'I didn't notice one' is an explicit valid answer. | ASK THE USER what they see during scene 2 -- 350 tasks (~12 s) that submit ZERO objects and touch NO tables | A672/A679 measured scene 2 (tasks 4314-4663, between scene 20 and the tutorial) as completely inert: 0 objects reaching the cull, 0 accesses to tables A/B/C, ctl only. That is roughly twelve seconds of a run rendering nothing. It may be a legitimate loading/fade scene -- or it may be visibly wrong. I cannot tell from a log and MUST NOT guess (T101). One question on the next observed run settles it, and it costs the user nothing extra since they are watching anyway. |
| IDEA28 | 2026-08-28 | OPEN | The table at 0x80059280 is a 128-entry JUMP TABLE of per-type handlers, and it is how table A's accessor dispatches | Found closing out IDEA22. boot_func_8002CFAC takes a 7-bit type field from its argument (+0x06), shifts left 2 and indexes 0x80059280. In a590-tutorial.rdram that table has 99 non-zero entries; from index 3 onward they are function pointers at 0x8002CC70 / 0x8002CCE0 / 0x8002CD54 / 0x8002CDC8 / 0x8002CE3C / 0x8002CEB0 -- evenly spaced ~0x74 apart, adjacent to the emitter at 0x8002C12C. So this is dispatch-by-object-type, and it is ALSO the data-resident dispatch A670's control proved instruction scanning cannot see -- which makes it a concrete instance of that blind spot and a possible route into IDEA18/IDEA19. Entries 1 and 2 are 0x8000/0xC000, not pointers, so the table is mixed or my base is off by a couple of slots. UNVERIFIED, read in the last minutes of a timed session. |
| IDEA29 | 2026-08-28 | OPEN | The dispatch array at ~0x80103740 is 8-byte {param, handler} pairs, and several params are INCREASING FLOATS -- it looks like a scene script | Refines IDEA18's answer, found in the final minutes. The stride is 8, not 0x20: each entry is a u32 parameter followed by a u32 pointer to an ovlfile19 function. Most params are 0 or small ints (0x04, 0x10, 0x18, 0x24), but three are floats and they INCREASE down the array: 0x45960000 = 4800.0, 0x45C4E000 = 6300.0, 0x45F3C000 = 7800.0. That is the shape of a scene script or trigger list -- a threshold (distance? position along the corridor? time?) paired with the handler to run. The four functions that touch table B are entries in this array. If it IS a positional script, then WHERE the tutorial is in it would determine which handlers have run, which is a plausible route to why the table pointer is set the way it is. UNVERIFIED and speculative about the float meaning; the layout itself is read directly from a590-tutorial.rdram. |
| IDEA30 | 2026-08-28 | OPEN | The 0x8013C7C0 cluster is a LIVE doubly-linked list of activated script entries, distinct from the static array at 0x80103740 | Final observation of the session. Only 5 of 64 words match the 0x80103740 region, so it is not a copy. Its records are 32 bytes with what read as forward AND backward links (each record holds two nearby 0x8013Cxxx addresses ~0x20 apart in opposite directions), a handler pointer to the same ovlfile19 functions, a constant 0x00000010 or 0x00000020, a 0x00000001, and a packed value that DECREASES down the list: 4B594B59, 32953295, 19CF1A0F, 198D19CF, 194B1909. So the picture is a static script (0x80103740, {param, handler} pairs, increasing float params) plus a live list of entries that have been activated from it. If that is right, WHICH entries are live in the tutorial versus scene 20 is directly inspectable and may be the shortest path to why the table pointer differs. UNVERIFIED: record alignment is off by 0x10 in my reading, and the link interpretation is inferred from address adjacency, not from code. |
