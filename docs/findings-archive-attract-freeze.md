# Archive — attract-mode freeze & stack-drift hunt (CLOSED)

Moved out of `docs/findings-ledger.md` on 2026-08-19 under T32's rules.

**This is not deleted history and not a lesser tier — it is the same evidence,
moved because the investigation it belongs to is CLOSED.** The attract freeze
was root-caused and fixed (**L7**, user-confirmed on screen) and the START
crash before it (**L1**). What remains here is the working-out.

**`scripts/check_ledger.py` reads this file**, so citations of these IDs from
the live ledger still resolve and do NOT count as dangling (T21). Cite them
freely.

**What deliberately did NOT move**, per T32:
* every **WITHDRAWN** row — the retracted beliefs are the highest
  value-per-byte content in the ledger and stopped two attempted resurrections
* every **I-series** and **T-series** row — the transferable lessons
* every **OPEN** row
* every row still **cited by live work** — e.g. A6/A11/A15 describe the scene
  walker's data structure and A107 depends on them today

| # | status | finding | evidence |
|---|---|---|---|
| A3 | EST | Baseline before the walk = 5,488 bytes — **not** inflated | 2026-08-18; identical on relocated stack |
| A5a | MEASURED | The child data of node `0x80102784` is **static** — confirmed twice by different methods (`SNP_WATCH` sampling, then two watchpoints with zero hits, L4) | 2026-08-18 |
| A14 | EST | The walker's call rate collapses at **t≈32** (2790/s -> 186/s), a full 10s **before** the first cycle and while graphics are still perfect. Most likely benign — attract switches to a smaller scene — but it must be excluded before reading t≈32 as the trigger | 2026-08-18 |
| A17 | EST | **The depth growth is REAL, not a counter artefact.** `low_sp` — an actual observed minimum stack pointer, not a tally — falls from `0x8007B748` to `0x8007AFE8`, i.e. **1,880 extra bytes** of stack consumed, and lands 220 bytes above the dispatch table it goes on to clobber. Any explanation must account for genuinely deeper recursion | 2026-08-18; `SNP_WALK` low-water |
| A19 | MEASURED | **The walk's root changes exactly once, and early.** Per-frame root: `800E7D98` with **70** calls/frame up to frame ≈420, then `800EC8D4` with **93** calls/frame, both then constant. The switch is at **t≈14s — some 25s BEFORE the failure** (t≈39). `800EC8D4` is the same node that heads the cycle report's ancestor path | 2026-08-18; `[root]` probe |
| A20 | MEASURED | Calls-per-frame is **flat at 93** from the root switch through at least frame 750 (t≈25s). No gradual climb in walker work during that window | 2026-08-18 |
| A21 | **INCONCLUSIVE — instrument limit** | **The `[root]` probe says nothing about the failure window.** Its output stops at frame 750 (t≈25s), well before the t≈39 event. Its top-level detection is `sp + 64 >= max_sp_ever`, so once recursion deepens and a top-level call arrives at a slightly lower sp, frames stop being counted and it silently goes quiet. **Do not read the absence of later lines as "the walk stopped."** Fix: detect frame boundaries from the *caller* (`func_800261FC`), not from a stack-pointer heuristic | 2026-08-18 |
| A23 | MEASURED | **Top-level walk calls cease at n≈780 (t≈26s)** — while graphics continue happily to t=42. Two independent probes now agree the walk stops long before the freeze, so this is not the earlier probe's heuristic failing (A21) | 2026-08-18; `[top]` probe, wrapper-hooked |
| A31 | MEASURED | Per-traversal `maxdepth` is **9 on every traversal sampled** (t≈0-20s), with `calls` stepping 70 -> 93 at the root change. No traversal in that window is deep. **Scope: healthy window only** — the probe stopped reporting before the failure (A30), so this says nothing about t>20s | 2026-08-18 |
| A29 | INFERRED | **The arithmetic does not add up yet, and that is the live lead.** Depth is bounded by the longest forward chain, and the dumped subtree spans only 22 slots — so **90 levels cannot come from it**. Either the deep descent happens in a different, larger subtree than the one dumped, or the level counter is inflated by stale shadow-stack entries. `low_sp` falling 1,880 bytes at roughly 0x50 per frame is ~23 extra levels, **not 90** — so the counter and the stack disagree by about 4x | 2026-08-18 |
| A32 | MEASURED | **With per-thread state, thread 4's walk is healthy for as long as the probe can see it.** Every sampled traversal: `maxdepth=9`, entry sp a constant `0x8007B9C8`, `calls` 70 then 93 after the root change — through traversal 742 and ~60,000 calls (t≈25s). No traversal in that range is deep, and none fails to complete | 2026-08-18; `[trav2]`/`[live]` probes, `_Thread_local` |
| A33 | EST | **Probe-design lesson, not a finding about the game:** sampling every 20,000 calls is too coarse for this failure. The walk totals ~62-79k calls, so the deep descent lives in the **final few seconds / last few thousand calls**, and a fixed-interval sampler prints nothing there. Sample by *time near the end* or trigger on `maxdepth > 9`, not on a call-count modulus | 2026-08-18 |
| A34 | EST | **Boundary detection should be self-correcting.** Marking a traversal by "the shadow stack emptied" needs no constant and keeps working if the entry depth moves — unlike the hard-coded `sp >= 0x8007B9C8`, which silently stops marking anything the moment the assumption breaks. That failure mode is what produced the withdrawn A27, which this **corrects** | 2026-08-18 |
| A35 | MEASURED | **THE DESCENT, CAUGHT EXACTLY.** Anomaly-triggered probe, 81 depth records: depth climbs **9 -> 90 entirely inside ONE traversal (trav=750)**, and every single record is at **the same node `0x801028EC`**. Perfectly regular: sp falls by exactly **`0x18` per level**, and there are exactly **23 calls between consecutive levels** (nc 60822, 60845, 60868 … 62662). So one descent re-enters `0x801028EC` 80 times, walking 23 nodes each time | 2026-08-18; `[deep]` probe |
| A37 | MEASURED | **Answered — and it disproved its own premise.** The dump showed the accused node is a leaf with a NULL child list, so there is no edge to name and no loop to close. Superseded by A38/A39; the live question is now A40. Original: **Next, and narrow:** at a depth record, dump the ancestor chain and `0x801028EC`'s child-list pointer + bytes (with the `^3` swap). That names the edge that closes the loop. Everything needed is now pinned: traversal 750, node `0x801028EC`, ~23 calls per level, `0x18` of stack per level |  |
| A41 | MEASURED | **A free ROM-wide check for unbalanced stack adjustments now exists.** Scanning `RecompiledFuncs/` for `ctx->r29 = ADD32(ctx->r29, N)` per function: **244 of 6,827** decrement `$sp` with no matching positive of equal magnitude, **95 of them by exactly `0x18`**. Reusable for the whole class | 2026-08-18 |
| A43 | NEGATIVE(scene-walk path, whole-function scope) | **A whole-function missing restore does not explain the stack shortfall** (this **supersedes** the reasoning that leaned on the withdrawn A39, but the negative itself is independent of it). Of the 95 exactly-`0x18` candidates, **none is on the scene-walk path** — the walker's callees are boot-segment (`0x80027xxx`/`0x80032xxx`) plus the type vtable at `D_80059748`, and the only boot entry in the list is the non-returning thread loop. Scope stated because the check only asks whether *any* matching restore exists anywhere in the function | 2026-08-18 |
| A46 | MEASURED | The wrapper's entry `$sp` is **constant for the first 750 frames** and only then begins drifting — matching A35's descent onset at trav=750 exactly. **The leak is triggered by something at frame ~750, not present from boot** | 2026-08-18 |
| A47 | EST | Startup transient, not a leak: `func_800261FC`'s own entry `$sp` moves -0x20 on calls n=2 and n=3 only, then is stable. Do not mistake it for the defect | 2026-08-18 |
| A70 | NEGATIVE(main + boot segments, generated C, static return/restore counts) | **A44 is closed: the early-return-skips-restore model is dead on the whole walk path, not just half of it.** A51 tested this over the **boot** segment; A67 then showed part of the path (`main_func_800BA1A4`, `main_func_800BA094`) is **main**-segment, so A51's scope never covered it. Re-run over the main segment: of 620 functions, **463 allocate a frame, and 0 have more `jr $ra` return points than positive `addiu $sp` restores**. Boot segment independently recomputes to 0 as well. The 48 ROM-wide hits are all `ovlfile*` overlays — **and calling those "off the path" was an assumption, not a measurement: A85 found the defect is exactly one of them, dispatched every frame.** The segment negatives stand; the dismissal of the overlay hits does not. **Positive control:** the same counter returns 48 non-zero hits ROM-wide and reproduces ground truth for `main_func_800BA1A4` (`1 return / 1 restore / 1 alloc`), so the zero is a real absence, not a dead regex. **Method limit:** this compares static *counts*, so a function that restores on every path but with mismatched counts would be missed — it does not prove per-path balance. **Discrepancy recorded:** A51's figure of "219" does not reproduce by any variant tried (968 unfiltered, of which 920 are leaves that never allocate; 48 genuine). A51's conclusion stands and is now wider; its number should not be reused | 2026-08-18; static pass over `RecompiledFuncs/` |
| A69 | MEASURED + INFERRED | **Predicted death frame ~1108, and the run is consistent with it.** MEASURED: heartbeats printed at frames 0/256/512/768/1024 and **no heartbeat at 1280**, despite ~70s of the 300s run remaining — so frame advance stopped or collapsed somewhere in `(1024, 1280)`. INFERRED (arithmetic, not observed): `0x8007B9E0` down to the dispatch table at `0x8007AF0C` is `0xAD4` = ~116 frames at `0x18`/frame, putting the overrun at frame **~1108** — inside that gap. **Falsifier:** a heartbeat at 1280 on a later run, or the overrun landing outside `(1024, 1280)` when measured directly. **Not yet checked** — today's honest reading is only "frames stopped advancing between 1024 and 1280"; the cause is unmeasured. Cheap check: heartbeat every 32 frames, read the last one | 2026-08-18; `[spw]` heartbeat, 300s run |
| A84 | MEASURED | **The per-frame callback list, in dispatch order — 10 callbacks, and `0x800BA1A4` is slot 6.** `800B4CE8`, `800E4C2C`, `800BA8B0`, `8003C538`, `800BA6DC`, **`800BA1A4`**, `800C03D0`, `800BCD18`, `800A9BF4`, `800A8638`, repeating exactly every 10 dispatches. **This is the caller/callee link A45 assumed and was withdrawn for, now measured**: `main_func_800BA1A4` is reached as a per-frame callback dispatched from `boot_func_80026A54`. It also corrects A82's remark that `0x800BA1A4` "was not among" the targets — it is; A82 only ever sampled the slot occupied at the moment `sp` changed. Scope: attract sequence, this dispatch loop | 2026-08-18; `seq.log` |
| A82 | MEASURED | **The descent is already present at the per-frame callback dispatch, and the frame does 10 dispatches.** One cold hook at `0x80026AC8` (`jalr $v0` in `boot_func_80026A54`), no per-call RDRAM read. `sp` holds constant for **exactly 10 consecutive dispatches, then falls `0x18`**, repeatedly and without exception across the sampled window. **Run control passed** — gfx `2649` at `+30/s`, matching an uninstrumented run, so unlike I11/I13 this probe did not perturb the game. Targets seen at the moment of each drop: `0x800BA8B0`. **Do not read that as `0x800BA1A4` being absent — A84 measured the full 10-slot list and it is slot 6.** This probe only ever sampled whichever slot happened to be executing when `sp` changed | 2026-08-18; `cb.log` |
| A83 | READ + MEASURED | **The loss is a callee that returns with `sp` lower than it received — and it is NOT any of the three functions on the path.** `boot_func_80026288` is the **sole** caller of `boot_func_80026A54` (xref over `RecompiledFuncs/`, the complete index) and is balanced: `-0x18` at entry, `+0x18` at `0x80026334`, one `jr`, and **no `sp` op anywhere inside its loop**. Within a single invocation, `sp` at a loop head therefore cannot move unless a **callee returns lower than it was called with**. `boot_func_80026A54` is balanced too (`-0x18`/`+0x18`, one return), as is the dispatched `main_func_800BA8B0` (`0x800BA8B0`/`0x800BA984`, one return). So the loss is **inside a dispatched callback's subtree**. **Note this is exactly the gap A70 named and could not close** — its static per-function counts cannot see per-path imbalance | 2026-08-18 |
| A81 | MEASURED | **The walk's per-frame stack footprint is CONSTANT; the whole chain just sits lower each frame.** New per-frame `sp` extremes (reset with the frame proxy, unlike the cumulative `min_sp`/`max_sp` that misled A78): healthy t=7-35 gives `fmax=0x8073EA90` dead constant, `fdepth=640`. From the onset, `fmax` descends steadily — `0x8073E2C8` (t=42) -> `0x8073BB68` (t=56) -> `0x80737230` (t=84), about **686 bytes/sec = `0x18` per frame at 30fps** — while **`fdepth` stays 560-640, i.e. unchanged from healthy**. Run's gfx control healthy throughout (`2653`, `+30/s`). **So the walker's subtree neither deepens nor widens; it is simply entered `0x18` lower every frame.** Combined with A71 (`sp` restored arithmetically everywhere) this puts the loss **strictly above the walker**, and A67 already showed `main_func_800BA1A4`'s body is stack-neutral, so it is above that too | 2026-08-18; `fsp.log` |
| A78 | MEASURED | **A72/A74/A75 are REFUTED: they were SHADOW-STACK ARTIFACTS — the same defect class as I2.** The probe pops entries with `back.first <= sp`. Correct within one descent; but the walker's own entry `sp` falls `0x18` per frame, so the **previous** frame's entries sit at a HIGHER sp and can never be popped. One stale entry accumulates per frame, which manufactures **exactly** "+1 level per frame" and one "revisited ancestor" per frame — i.e. it generates the very signature A72 read as proof of depth growth, and A74's `cycles = levels - 9` identity is just "9 real levels plus one artifact per frame". **`depth` and `low_sp` are cumulative over the whole run, never reset, so `depth=32784` was never a per-frame depth either.** Two independent instruments agreeing (A72) meant only that both were fed by the same falling `sp` | 2026-08-18 |
| A79 | MEASURED | **The walk root DOES change at the onset — A18's first alternative.** With framing on: root is `0x800E7D98`, then `0x800EC8D4` from t~21 (both healthy, `levels=9`), then changes to **`0x80102784` at t~42, exactly at the onset**, and keeps changing thereafter (`rootchg` 3 -> 5 -> 262 by t=84). The earlier unframed run showed `rootchg=2` and a frozen `roots` **only because top-level entries stopped being detected at all** once the stale path never emptied. Note `0x80102784` sits in the same neighbourhood as L4's watched `0x8010273C`/`0x8010278C` | 2026-08-18 |
| A76 | MEASURED — **needs a control** | **`SNP_STACK_RELOC=4` DEFERS the stall, it does not remove it.** Same run: gfx held a perfect **+30/s from t=2 to t=149**, then collapsed to **+0/+3/+2** at t=150 and never recovered through t=300; the scene walker's call count froze at 209,569 at the same moment. That refines A2's "runs past 1240 to 2650+, **never stalls**" — it reached 4,833 gfx tasks, but it did stall, at t=150 rather than t=43. **Not yet attributable:** this run carried `SNP_WALK`, whose shadow stack does real work on every one of 209k walker calls, so probe overhead is a live alternative explanation. **Control to run:** `SNP_STACK_RELOC=4` with `SNP_HEARTBEAT=1` and **no** `SNP_WALK`, 300s, and see whether the t=150 collapse survives | 2026-08-18 |
| A50 | MEASURED | **The wrapper is entered exactly ONCE per frame — nesting refuted.** `entries=1` at every sampled frame, and `minsp` constant at `0x8007B9E0`. The frame counter itself stops once drift begins, because the boundary test is `sp == max` and **`sp` never returns to its maximum again** — which is independent confirmation that this is a true leak, not deeper nesting | 2026-08-18; `[per]` probe |
| A52 | **INCONCLUSIVE** | Read 48 words at the node-type vtable `0x80059748`: 36 look like pointers, 6 resolve to no recompiled function. **But three of those six (`800D4F00`, `800D5A70`, `800E4FE0`) are known DATA globals**, so the read plainly overran the table's real end. **The table length is unknown and must be established before this means anything** | 2026-08-18 |
| A58 | READ | **An unresolved indirect call cannot silently leak — it kills the process.** `get_function()` in `librecomp/src/overlays.cpp` prints `Failed to find function at 0x…`, asserts, and `exit(EXIT_FAILURE)`. No such line appears in any run log. So "the walker dispatches a bogus pointer that quietly no-ops" is refuted for free | 2026-08-18 |
| A59 | EST | **The attract-mode caller set is tiny.** Of the ~63 callers of `func_80033A40`, all overlay ones are `ovlfile04`-`ovlfile22` — **`ovlfile23`, the attract scene, is not among them.** So during attract the wrapper can only be reached from the three `main` functions `800B09EC`, `800BA1A4`, `800C8928`. **All three are in the `.main` segment `decomp.sh` cannot read (T10/A26)** — which is now blocking the live investigation, not just tidiness | 2026-08-18 |
| A60 | MEASURED | **The drift is isolated to ONE function.** Same run, three levels: `func_80026288` (driver) and `func_80026A54` (callback walker) drift **only during startup** (n=2-11) and are **stable** thereafter; `main_func_800BA1A4` drifts **-0x18 per call from n=992** onward. So the leak is in that function or below it — not in the resident driver or the callback walk | 2026-08-18; `[drv88]`/`[walk54]`/`[cbA1A4]` |
| A63 | MEASURED | **Called exactly once per frame** — `n` tracks the game's own frame counter `D_800681EC` precisely (200/200, 400/400, 600/600, 800/800, 1000/1000). So A62(1) is settled and the frame model holds | 2026-08-18; `[cpf]` probe |
| A65 | NEGATIVE(scene, gate, selected group, attract state) | **The onset is not explained by any state global I sampled.** Across frames 975-1015, `D_80068A95` (scene) = 0, `D_80068A97` (gate) = 0, `D_80068180` (selected group) = `0x80067CA0` and the attract state byte = 41 — **all unchanged**, including across the exact frame the leak begins | 2026-08-18 |
