# Small owed jobs

**For genuine remainder time only.** The frontier comes first, always: this
exists because a session with ten minutes left and no big job that fits used to
close early (T161), not because tidying is worth doing instead of the work.

**Every item here is something already OWED** — a count that drifted, a check
not re-run since its script changed, a flag left unanswered, evidence not yet
preserved. **Nothing invented.** If an item cannot be shown to be done or not
done, it does not belong here.

Written and read by `scripts/backlog.py`. Add with `add`, close with `close`. **IDs are `BL<n>`** — the ledger has its own `B` family and the two collided (T171).

| id | opened | status | job | why it is owed |
|---|---|---|---|---|
| BL1 | 2026-08-22 | OPEN | The `session-log.md` row for the 12:25 session records 3m27s for a stretch that ran ~20 minutes | Marked unreliable in the file; cause unknown. The obvious explanation (self-check clobbering live state) was checked and ruled out |
| BL2 | 2026-08-22 | OPEN | Re-check every inventory row in `diagnostic-playbook.md` that states a control count | The `observed_run.sh` row said 5 when it was 8 (T150 found it, T160 fixed it). One drifted, so the class recurs |
| BL3 | 2026-08-22 | OPEN | Adopt the context audit from T163 — run `/context` at session start and drop MCP servers this project never uses | Read and recorded, never acted on |
| BL4 | 2026-08-22 | OPEN | Add the subagent CONTEXT-ISOLATION rationale to `docs/agent-brief.md` | T163 found it absent: the brief argues delegation only, and isolation is a separate reason to hand off a bulky read |
| BL5 | 2026-08-22 | CLOSED 2026-08-22 | Add `evidence/` to `.gitignore` | A checkpoint wrote three run logs into the repo by accident; they were moved to the archive and the mistake made non-committable |
| BL6 | 2026-08-22 | OPEN | Sweep the census walker for every pointer-valued operand and whether it passes through resolve() | A356 claims G_SETCIMG is the ONLY one that skips it, and that negative rests on a single read of one file by me |
| BL7 | 2026-08-22 | CLOSED 2026-08-23 | Extract the claim cell alone from each ledger entry, excluding the method prose | A358 named this as the untested better-classifier route after its keyword version failed its own control at 56% |
| BL8 | 2026-08-22 | OPEN | Give scripts/audit.py a --self-check with controls VERIFIED TO FAIL | T171: the L1 level of the review ladder is the only substantial tool here with no control at all, and it advises halving its own frequency on a quiet streak nothing can validate |
| BL9 | 2026-08-23 | OPEN | Capture a dense census (interval 301) WITH the cycle= field to test colour clears on all three buffer phases | A367: stall-dense samples densely enough but predates the cycle field; cycle.log has the field only at ~300 spacing, which is one phase in three. A356's colour-clear counts are unverified on the other two |
| BL10 | 2026-08-23 | CLOSED 2026-08-23 | Design a confidence-and-scoring field for load-bearing claims, or the smaller rule that replaces it | T177: I proposed calibration tracking in conversation and never recorded it; the gap is real (five instances this session, three caught by the user rather than by any check) but 32% of entries are already method, so the cheaper rule may be better |
| BL11 | 2026-08-23 | OPEN | Fix the shared SUPERSEDES_RE: it misses CORRECTED and scope-flagged, and its 220-char window cannot tolerate adding them | T179 measured it: 39 supersessions invisible, but 3 of 4 newly-found were false positives from the window. The vocabulary gap and the loose window mask each other, and audit.py depends on both |
| BL12 | 2026-08-23 | OPEN | Run the two sub-agent sweeps T170 cleared as ready: the logo-archive retrieval and the instanced-children retrieval over RecompiledFuncs | T170 seeded both from ground truth (A355's DMA logs, A344/A348/A353's census) and priced verification at ~1 read per claimed site; they were scoped, cleared, and then never run |
| BL13 | 2026-08-24 | OPEN | Fix ledger.py's circle-warning control: it FAILS to discriminate — A99 and A97 both return False, so a control written to fire on one and stay silent on the other now fires on neither | It is a control VERIFIED TO FAIL that has silently stopped discriminating. Confirmed pre-existing at HEAD (2026-08-24, T186) and not caused by that checkpoint, so nothing is corrupted — but T100 says a control that stops discriminating is the standard way a checker goes quiet, and until it is fixed --self-check reports 13/14 every run, which trains skimming |
| BL14 | 2026-08-24 | OPEN | status_page.py's 'ideas section READS without the runtime' control is LOCATION-DEPENDENT: it passes from the repo root and fails when the same unmodified script runs from another directory | Found while break-testing on 2026-08-24 (T187). A control whose outcome depends on where you stand is not testing what it says; it made two deliberate breaks harder to read, and would read as a real defect to anyone running the script from elsewhere |
| BL15 | 2026-08-24 | OPEN | Add a check that a commit message naming an entry ID (T123, A456) has a matching row in the ledger | T192 shipped as code in commit 9440e62 naming T192 while no T192 row existed. check_ledger.py validates rows that EXIST and structurally cannot catch a promised-but-unwritten entry. It was found by accident, and it is mechanically detectable |
| BL16 | 2026-08-24 | OPEN | Write the ideas decisions back to a project file the way T193 did for labels — approve/decline currently live ONLY on the published page | T193 fixed labels and named this as the same unfixed bug. An idea decision is read back by hand and nothing writes it into docs/IDEAS.md automatically, so a regeneration can discard it exactly as it would have discarded A357 |
| BL17 | 2026-08-24 | OPEN | Enumerate the other scheduled/cron jobs and give each a detector that is not 'a line in a log file' | T194: the nightly push failed silently for three days and 182 commits; the ONLY thing that surfaced it was the L1 audit printing the last cron line. Nothing else here detects a scheduled job that refuses |
| BL18 | 2026-08-24 | CLOSED 2026-08-24 | Run one capture with SNP_WATCH on 0x80068A94 carried past t=210s, logged beside the colour-clear signature | A386 priced it: it answers three open questions at once — whether A374's guard byte flips at all, whether at 145s or 207s, and whether the clear-stop (A385) coincides with either. No post-cutoff capture watches that word; the only two watch a different address and stop short |
| BL19 | 2026-08-24 | OPEN | Find the asset table that maps archive index to ROM offset, then the code that indexes it | A387: the logo archive's ROM offset 0x00A82C50 is not a literal anywhere in the generated C, so finding the loader by constant search is dead. A237's named next step (which function should submit the logo draws) needs the table route instead, and it has not been started |
| BL20 | 2026-08-24 | OPEN | Test the object-vs-level framing on A218: are the girders that DO render submitted by a different route (instanced children) than the deck and buildings that do not? | A389 named it as a description rather than a mechanism. The instanced-children work (A344/A353/A361) already measured how per-object geometry is submitted, so the comparison may be answerable from existing captures without a run |
| BL21 | 2026-08-24 | OPEN | Trace boot_func_800263CC's control flow properly: can it exit before the line that clears the guard byte? | A393 counted four returns with three earlier in the file, but generated C is full of gotos so textual order is not flow order. This is the function that decides whether the graphics-off request is ever cleared, and it is ~290 lines |
| BL22 | 2026-08-24 | OPEN | Read the Drive-backup cron check in audit.py for the same log-reading defect T196 fixed in the push check | T196 fixed the push check to measure state rather than read its log, and explicitly did not read the backup block below it. That job carries the ONLY off-machine copy of the probe patches, which cannot go to GitHub at all |
